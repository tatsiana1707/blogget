const container = {
  display: 'grid',
  textAlign: 'center',
  margin: '33% 0px',
  gap: '38px',
};

const Home = () => (
  <div style={container}>
    <h1> Стартовая страница </h1>
    <p>Добро пожаловать!</p>
    <p>Выберите категорию</p>
  </div>
);

export default Home;
